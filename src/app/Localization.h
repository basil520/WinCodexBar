#pragma once

#include <QString>

namespace Localization {

QString providerLabel(const QString& source);
QString providerSettingLabel(const QString& source);
QString providerError(const QString& source);

}
